//@version=5
indicator("EMA 100 / 200 / 300", overlay=true)

// Inputs
len1 = input.int(100, "EMA 1 Length")
len2 = input.int(200, "EMA 2 Length")
len3 = input.int(300, "EMA 3 Length")

// EMAs
ema1 = ta.ema(close, len1)
ema2 = ta.ema(close, len2)
ema3 = ta.ema(close, len3)

// Plots
plot(ema1, "EMA 100", color=color.yellow, linewidth=2)
plot(ema2, "EMA 200", color=color.blue,   linewidth=2)
plot(ema3, "EMA 300", color=color.red,    linewidth=2)
