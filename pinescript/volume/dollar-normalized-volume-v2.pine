//@version=5
indicator("Dollar Normalized Volume v2", shorttitle="DNV v2", format = format.volume)

getVolume(closed, volumed) => closed * volumed

val = getVolume(close, volume)
plot(val, title="Volume", color=color.gray, linewidth=1, style=plot.style_columns)
