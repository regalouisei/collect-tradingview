//@version=5
// @description Visualizes the monetary trading value (Price * Volume) with auto-scaling units (K, M, B).
indicator("USD Volume (Auto-Unit Scaling)", shorttitle="USD Vol", overlay=false, format=format.volume)

// 1. Calculate Trading Value
// Represents the total fiat/monetary value traded in each bar.
tradingValue = close * volume

// 2. Plotting
// 'trackprice=true' enables the live value label on the Y-axis.
plot(tradingValue, 
     title="Current Trading Value", 
     style=plot.style_columns, 
     color=close >= open ? color.new(#26a69a, 30) : color.new(#ef5350, 30),
     trackprice=true)
