//@version=6
indicator("Distance % from sma")

hline(0, color=color.red, linestyle=hline.style_solid, title="neutral")

smaLen = input.int(36, title="SMA Length")
price = close

ma = ta.sma(price, smaLen)

distFromMean = (price - ma) / ma * 100

plot(distFromMean, color=color.black, style=plot.style_stepline)
